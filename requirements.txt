streamlit==1.25.0
TTS==0.14.1          # Coqui TTS - check latest; install may require torch
torch>=2.1.0         # CPU or CUDA version depending on host
pydub
soundfile
numpy
requests             # if using OpenTTS server option
python-dotenv
